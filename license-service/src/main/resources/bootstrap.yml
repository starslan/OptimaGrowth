spring:
    application:
        name: license-service
    profiles:
        active: dev
    cloud:
        config:
            uri: http://localhost:8071
    jpa:
        show-sql: true
        properties:
            hibernate:
                format_sql: true

server:
    port: 8075
